#!/usr/bin/env bash
# Routism CLI wrapper — runs from any cwd; module discovery uses this repo.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="${ROOT}${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m routism_cli "$@"
