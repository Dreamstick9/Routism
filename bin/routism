#!/usr/bin/env bash
# Routism CLI — run from anywhere if this file is on your PATH.
# Prefer:  ./install.sh   then just type:  routism
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="${ROOT}${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m routism_cli "$@"
