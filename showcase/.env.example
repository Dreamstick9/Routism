# Public URL of this showcase (used for metadataBase / Open Graph absolute URLs).
# Leave empty or unset for local dev — layout falls back to http://localhost:3100.
# Set at deploy when the production domain is known. Do not invent a domain.
NEXT_PUBLIC_SHOWCASE_URL=
